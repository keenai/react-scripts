{
  "collectCoverageFrom": [
    "<rootDir>/src/*.{js,jsx}"
  ],
  "coverageDirectory": "<rootDir>/coverage",
  "testMatch": [
    "<rootDir>/src/**/__tests__/**/*.js?(x)",
    "<rootDir>/src/**/?(*.)(spec|test).js?(x)"
  ]
}
